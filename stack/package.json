{
  "files": [
    "library"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "debug": "NODE_ENV=test ts-node-dev --inspect --respawn ./node_modules/.bin/tape tests/github.spec.ts | tap-nirvana",
    "test": "NODE_ENV=test ts-node ./node_modules/.bin/tape tests/**/*.spec.ts | tap-nirvana",
    "test:watch": "NODE_ENV=test ts-node-dev --respawn ./node_modules/.bin/tape tests/**/*.spec.ts | tap-nirvana",
    "test:console": "npm run build && RUN_CONSOLE_TEST=1 ts-node ./tests/console-runner.spec.ts",
    "test:update": "UPDATE_SNAPSHOTS=1 npm run test",
    "report": "NODE_ENV=test nyc --require ts-node/register tape **/*.spec.ts -x **/library/** | tap-nirvana"
  },
  "devDependencies": {
    "@types/node": "^10.12.9",
    "@types/ramda": "^0.25.43",
    "colors": "^1.3.3",
    "diff": "^3.5.0",
    "nyc": "^13.3.0",
    "rimraf": "^2.6.2",
    "riteway": "^4.0.1",
    "tap-nirvana": "^1.1.0",
    "tap-spec": "^5.0.0",
    "tape": "^4.9.1",
    "tmp": "0.0.33",
    "ts-node": "^7.0.1",
    "ts-node-dev": "^1.0.0-pre.32",
    "typescript": "^3.1.6"
  }
}
